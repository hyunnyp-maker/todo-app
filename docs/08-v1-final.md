# 08. v1 최종 명세 — 계획과 실제

> [01 요구사항](./01-requirements.md)은 **착수 전에 쓴 문서**다. 그대로 둔다.
> 이 문서는 **완성 시점의 실제 결과**이며, 계획이 어디서 어떻게 움직였는지를 함께 남긴다.

기준 커밋: `08a48ab` · 마일스톤 M1~M10 완료
배포: https://todo-app-two-jet-82.vercel.app

---

## 1. 계획이 움직인 지점

가장 크게 달라진 것은 **7장 "범위 제외"에 넣었던 항목 다섯 개를 결국 만들었다**는 점이다.
빼기로 한 판단이 틀려서가 아니라, v1 골격이 예산보다 일찍 끝나 남은 시간을 여기에 썼다.

| 항목 | 01번의 계획 | v1 실제 | 왜 바뀌었나 |
|---|---|---|---|
| 규칙 기반 반복 | **제외** — 지속형(`daily`)의 날짜별 체크로 대체 | **구현** (매일 · 평일 · 매주 요일 · 매월 날짜) | "매주 월요일 회의"를 기간형으로 흉내 낼 방법이 없었다. 규칙 한 줄(`jsonb`)만 저장하고 뜨는 날은 조회 때 계산해, 우려했던 반복 엔진의 무게를 피했다 |
| 알림 | **제외** — 브라우저 상시 실행을 전제할 수 없음 | **구현** (정시 · 10분 전 · 1시간 전 · 하루 전) | 전제는 여전히 맞다. 그래서 "완전한 알림"을 만들지 않고, **한계를 UI에 그대로 적어** 켜는 사람이 알고 켜게 했다 (4.3) |
| 검색 | **제외** — 카테고리 색 구분이 핵심 | **구현** (제목 부분 일치) | 색은 **보고 있는 달** 안에서만 작동한다. 석 달 전 할일은 색으로 못 찾는다 |
| 내보내기 · 가져오기 | v2 후보 | **구현** (JSON 백업/복원) | 게스트 모드가 localStorage인 이상, 브라우저를 지우면 전부 사라진다. 게스트를 진짜 기능으로 쓰게 하려면 탈출구가 필요했다 |
| 다크 모드 | "여유 있으면 재검토" | **구현** (라이트 · 다크 · 시스템) | 색 토큰을 CSS 변수로 짜 둔 덕에 값만 한 벌 더 만들면 됐다 |
| 팔레트 | **파스텔** 9색 | **비비드 18색** + 대비 보정 토큰 | 파스텔은 채도가 낮아 색각 이상에서 구분이 어렵고(6.2에 스스로 적어둔 문제), 9색으로는 카테고리 10개째부터 색이 겹쳤다 |
| 음성 입력 | 항목 자체가 없음 | **구현** (음성 → 파싱 → 확인 → 저장) | "입력 마찰 제거"가 이 앱의 1번 목표(1.2)인데, 타이핑보다 마찰이 낮은 경로가 남아 있었다 |
| Google 로그인 | F0-4 | **제거** | OAuth 클라이언트 등록에 도메인 검증이 필요해 포트폴리오 배포 일정과 맞지 않았다. 이메일 + 게스트로 충분히 성립한다 |
| 스타일링 | "Phase 6에서 확정" | **Tailwind CSS v4 + CSS 변수** | |
| 배포 | "Vercel (예정)" | **Vercel** 확정 | |

**범위를 지킨 것** — 공유 · 다중 사용자 · 권한 · 충돌 병합 · 실시간 동기화 · 하위 체크리스트 ·
드래그 앤 드롭 · 주간/연간 뷰는 계획대로 v1에서 만들지 않았다.

---

## 2. 최종 데이터 모델

### 2.1 스키마

01번의 `categories` / `tasks`에서 **컬럼 세 개와 테이블 하나**가 늘었다.

```sql
-- 반복 · 알림 (0005)
alter table tasks
  add column recurrence    jsonb,                          -- null = 반복 없음
  add column reminder      text not null default 'none'
    check (reminder in ('none','at','10m','1h','1d')),
  add column reminder_time text not null default '09:00'
    check (reminder_time ~ '^([01][0-9]|2[0-3]):[0-5][0-9]$');

-- 반복 회차의 완료 기록
create table task_occurrences (
  task_id  uuid not null references tasks(id) on delete cascade,
  owner_id uuid not null default auth.uid() references auth.users(id) on delete cascade,
  date     date not null,
  primary key (task_id, date)          -- 같은 회차를 두 번 눌러도 행은 하나
);

-- 검색: ilike '%…%'는 인덱스를 타지 못한다
create index tasks_title_trgm_idx on tasks using gin (title gin_trgm_ops);

-- 복원: 전부 되거나 전부 안 되거나
create function restore_backup(payload jsonb) ... security invoker;
```

**설계 판단 세 가지**

1. **반복은 규칙만 저장한다.** '매일'을 1년치로 펼치면 한 사람이 365행을 쓰고,
   규칙을 고치면 그 365행을 다시 손봐야 한다. 뜨는 날은 조회 시점에 계산한다.
2. **완료 기록은 할일 본체와 분리한다.** 규칙을 바꿔도 지난 체크 이력이 살아남는다.
3. **복원은 서버 함수 한 트랜잭션.** 지우고 넣는 일을 클라이언트에서 나눠 하면
   중간에 끊겼을 때 데이터가 사라진 채 끝난다.

`recurrence`가 있는 할일도 `start_date` / `end_date`를 채워 둔다.
그래야 기존 범위 조회(`start_date <= :to and end_date >= :from`)가 그대로 통한다.

### 2.2 클라이언트 타입

```ts
type PaletteKey =
  | "red" | "orange" | "yellow" | "green" | "blue" | "pink" | "purple" | "brown" | "ink"
  | "lime" | "mint" | "sky" | "navy" | "indigo" | "wine" | "peach" | "khaki" | "gray";

type RecurrenceRule =
  | { type: "daily";    endDate?: ISODate }
  | { type: "weekdays"; endDate?: ISODate }
  | { type: "weekly";   daysOfWeek: number[]; endDate?: ISODate }   // 0=일 … 6=토
  | { type: "monthly";  dayOfMonth: number;   endDate?: ISODate };

type ReminderOffset = "none" | "at" | "10m" | "1h" | "1d";

interface Task {
  id: string;
  categoryId: string | null;        // null = 미분류
  title: string;
  memo?: string;
  startDate: ISODate;
  endDate: ISODate;
  checkMode: CheckMode;             // "once" | "daily"
  done: boolean;                    // once 전용
  completedDates: ISODate[];        // daily 전용
  recurrence: RecurrenceRule | null;
  reminder: ReminderOffset;
  reminderTime: string;             // "HH:MM"
  sortOrder: number;
  createdAt: string;
}

interface TaskCompletion { taskId: string; date: ISODate }   // 반복 회차 체크

interface GuestData {
  schemaVersion: 1;                 // 올리지 않는다 — 올리면 저장된 데이터를 못 읽는다
  categories: Category[];
  tasks: Task[];
  completions: TaskCompletion[];
  migrationAsked: boolean;
}
```

**완료 판정의 세 갈래** — 반복은 `checkMode`와 별개의 축이다.

| | 판정 |
|---|---|
| `once` | `done === true` |
| `daily` | 기간 내 모든 날짜가 `completedDates`에 있음 |
| 반복 있음 | `checkMode`를 보지 않고 `TaskCompletion` 기록으로 회차마다 판정 |

**팔레트 호환** — 새 색 이름은 예전에 쓰던 키(`sage` · `teal` · `mist` 등)와 겹치지 않게 골랐다.
저장된 옛 값이 조용히 다른 색으로 바뀌지 않고 기본색으로 정규화된다.

---

## 3. 기능 목록 (실제)

01번의 F0~F6은 그대로 두고, 실제로 만들어진 F7~F11을 잇는다.

### F0 인증

| ID | 기능 | 상태 |
|---|---|---|
| F0-1 | 게스트 이용 — 로그인 없이 전 기능 | ✅ |
| F0-2 · 3 | 이메일 가입 · 로그인 | ✅ |
| F0-4 | Google 로그인 | ❌ 제거 (1장) |
| F0-5 | 로그아웃 → 게스트 복귀, 캐시·큐 비움 | ✅ |
| F0-6 | 비밀번호 재설정 | ✅ |
| F0-7 | 게스트 데이터 이관 (병합, 한 번만 물음) | ✅ |

### F1~F6

달력 · 당일 리스트 · 할일 CRUD · 카테고리 관리 · 필터 · 데이터 영속 —
**01번 명세 그대로 구현**했다. 아래만 달라졌다.

- **F4-4 색상 변경** — 파스텔 팔레트 → 비비드 18색
- **F4-6 순서 변경** — "있으면 좋음"이었으나 드래그 재정렬로 구현
- **F5-1 카테고리 토글** — 하단 추가 바에서도 칩으로 노출

### F7 반복 일정

| ID | 기능 | 상세 |
|---|---|---|
| F7-1 | 규칙 지정 | 매일 · 평일 · 매주 특정 요일(복수) · 매월 특정 날짜 |
| F7-2 | 종료일 | 선택. 없으면 종료 없음 |
| F7-3 | 회차 체크 | 날짜별로 따로 남고, 규칙을 고쳐도 지난 기록이 살아 있음 |
| F7-4 | 수정 범위 | **전체 반복에 적용.** 단일 회차만 고치기는 v2 |
| F7-5 | 밀린 할일 제외 | 어제 못 한 '매일 운동'은 밀린 것이 아니다 |

### F8 알림

| ID | 기능 | 상세 |
|---|---|---|
| F8-1 | 시점 선택 | 없음(기본) · 정시 · 10분 전 · 1시간 전 · 하루 전 |
| F8-2 | 권한 요청 시점 | **알림을 켜는 순간에만.** 앱 시작 시 묻지 않는다 |
| F8-3 | 소리 | 켜기/끄기 + 미리 듣기. Web Audio로 생성 (오디오 파일 없음) |
| F8-4 | 한계 고지 | 아래 내용을 켜는 화면에 그대로 노출 |

> 웹 알림은 앱이 열려 있는 동안만 예약·발송된다. 탭을 닫으면 울리지 않고,
> 다시 열었을 때 최근 2시간 안에 지나간 것까지만 되살린다.
> 완전한 백그라운드 알림에는 Push API와 발송 서버가 필요하며 v1 범위 밖이다.
> iOS Safari는 홈 화면에 추가한 PWA에서만 알림을 허용한다.

### F9 검색

| ID | 기능 | 상세 |
|---|---|---|
| F9-1 | 제목 부분 일치 | 대소문자 무시, 앞뒤 공백 제거 |
| F9-2 | 범위 | 보고 있는 달에 갇히지 않고 저장소 전체 |
| F9-3 | 종류 무관 | 일반 · 기간형 · 반복 모두 |
| F9-4 | 닫기 | 날짜 필터와 달력 선택이 그대로 복귀 |

### F10 백업 · 복원

| ID | 기능 | 상세 |
|---|---|---|
| F10-1 | 내보내기 | JSON 파일. 카테고리 · 할일 · 반복 · 회차 기록 · 화면 설정 |
| F10-2 | **민감정보 제외** | 비밀번호 · 인증 토큰 · Supabase 세션은 **담지 않는다** |
| F10-3 | 가져오기 | 병합이 아니라 **전체 교체** |
| F10-4 | 검증 | 버전·구조를 먼저 확인하고, 무엇이 지워지는지 보여준 뒤 재확인 |
| F10-5 | 원자성 | 게스트는 단일 쓰기, 로그인은 `restore_backup` 한 트랜잭션 |

### F11 음성으로 추가

| ID | 기능 | 상세 |
|---|---|---|
| F11-1 | 음성 인식 | Web Speech API, `ko-KR`. 중간 결과를 입력창에 그대로 표시 |
| F11-2 | 권한 요청 시점 | **마이크 버튼을 누를 때만.** 앱 시작 시 요청하지 않는다 |
| F11-3 | 파싱 | **규칙 기반.** 외부 AI API를 부르지 않고 NLP 라이브러리도 넣지 않았다 |
| F11-4 | **자동 저장 금지** | 확인 화면에서 모든 칸을 고칠 수 있고, 저장 전까지 아무것도 만들어지지 않는다 |
| F11-5 | 저장 경로 | 기존 폼의 검증(`taskDraftError`)과 저장 흐름(`buildTask` → repository)을 그대로 쓴다. 음성 전용 경로 없음 |
| F11-6 | 미지원 대응 | 마이크 버튼이 꺼지고 안내가 뜨며, 손 입력은 그대로 열려 있다 |

**알아듣는 표현**

| 갈래 | 예 |
|---|---|
| 상대 날짜 | 오늘 · 내일 · 모레 · 다음 주 월요일 · 다음 달 10일 |
| 절대 날짜 | 8월 10일 · 2026년 8월 10일 · ~까지 · ~부터 ~까지 |
| 시간 | 오전 9시 · 오후 3시 · 14시 · 9시 30분 · 오후 3시 반 |
| 반복 | 매일 · 평일마다 · 매주 월요일 · 매주 화요일과 목요일 · 매월 25일 |

> **한계** — Chrome · Edge는 되고 Firefox는 되지 않는다. iOS · Safari는 중간 결과가 보이지 않을 수 있다.
> 인식은 네트워크를 쓰므로 연결이 없으면 실패하고, 결과가 항상 정확하지도 않다.
> 그래서 저장 전 확인 화면이 **선택이 아니라 필수**다.

### F12 화면 설정

| ID | 기능 | 상세 |
|---|---|---|
| F12-1 | 테마 | 라이트 · 다크 · 시스템 |
| F12-2 | 홈 화면에 추가 | 설치 가능한 브라우저에서는 버튼, iOS는 공유 시트 3단계 안내 |

---

## 4. 비기능 — 실제

01번 6장의 기준은 유지하되, 구현하며 확정된 값을 적는다.

| 항목 | 기준 | 실제 |
|---|---|---|
| 설계 기준 폭 | 375px | 유지. 달력 + 오늘 리스트가 스크롤 없이 들어간다 |
| 터치 타깃 | 44×44px | 유지 |
| 조회 범위 | 표시 중인 달 ± 여유 | **그리드 범위**로 조회 (7/26~9/5). 시작일로만 필터하면 달을 넘는 할일이 사라진다 |
| 색 구분 | 색만으로 구분하지 않음 | 카드에 카테고리 이름 항상 노출. 녹음 상태도 색이 아니라 글자로 함께 알림 |
| 텍스트 대비 | WCAG AA 4.5:1 | 색마다 토큰 네 벌(`bg` / `tx` / `dt` / `cp`). 원색을 글자에 그대로 쓰면 노랑이 1.3:1이라 읽히지 않는다 |
| 한글 IME | Enter 중복 방지 | `isComposing` 확인 |
| RLS | 모든 테이블 | `(select auth.uid())`로 감싸 쿼리당 한 번만 평가 |
| 웹폰트 | — | 로드하지 않는다. 첫 렌더 지연을 만들지 않기 위해 |
| iOS 자동 확대 | — | 터치 기기에서 입력 글자 16px. `maximum-scale=1`은 손가락 확대까지 막아 쓰지 않았다 |

**테스트** — Vitest, 11개 파일 212개.
`domain/`이 React도 Supabase도 모르는 순수 함수라 테스트가 여기 집중돼 있다.

```
domain/  date · task · category · recurrence · reminder · search · backup · voiceParse
data/    storage(손상 복구) · merge(재시도 큐)
ui/      VoiceInput (SpeechRecognition을 가짜 생성자로 대체)
```

---

## 5. 완료 기준 — 결과

01번 8장의 항목을 그대로 옮기고 결과를 적는다.

**인증**
- [x] 로그인 없이 앱의 모든 기능을 쓸 수 있다
- [x] 이메일 가입 · 로그인 · 로그아웃이 된다
- [ ] ~~Google 로그인이 된다~~ — 범위에서 제외 (1장)
- [x] 비밀번호 재설정 메일이 온다
- [x] 게스트로 만든 데이터가 로그인 시 계정으로 넘어간다
- [x] 이관을 거절하면 다시 묻지 않는다

**기능**
- [x] 달력 월 이동 · 오늘 강조
- [x] 여러 날에 걸친 할일이 이어져 표시된다
- [x] 날짜를 탭하면 아래 리스트가 바뀐다
- [x] 할일 추가 · 수정 · 삭제
- [x] 기본 입력이 하루짜리이고, 기간은 토글로 펼친다
- [x] 기간형 "매일 체크"가 날짜별로 따로 체크된다
- [x] 지속형 카드에 "12/31일" 진행률이 보인다
- [x] 체크가 달력과 리스트 양쪽에 즉시 반영된다
- [x] 카테고리 추가 · 이름수정 · 색변경 · 삭제 · 재정렬
- [x] 소속 할일이 있는 카테고리도 삭제 가능 ("미분류로 이동 / 함께 삭제")
- [x] 카드 배경이 카테고리 색으로 칠해진다
- [x] 카테고리 필터가 달력과 리스트에 함께 적용된다
- [x] 새로고침해도 데이터와 필터 상태가 유지된다

**품질**
- [x] 375px에서 가로 스크롤 없이 전 기능 사용
- [x] 모든 카드에 카테고리 이름 텍스트 노출
- [x] 한글 입력 Enter 중복 없음
- [x] 빈 제목 · 역순 날짜 차단
- [x] localStorage를 깨뜨려도 흰 화면으로 죽지 않는다
- [x] 네트워크를 끊고 열어도 마지막 데이터가 즉시 보인다
- [x] 오프라인에서 추가 · 체크가 되고 복귀 시 자동 전송된다
- [x] 대기 중인 변경에 표식이 보인다
- [x] 앱 시작 시 스피너가 화면을 막지 않는다
- [x] 다른 계정으로 로그인하면 남의 데이터가 보이지 않는다
- [x] `npm run build` 가 에러 · 경고 없이 통과한다

**포트폴리오**
- [x] README에 한 줄 소개와 배포 URL
- [x] 가입 없이 3초 안에 앱을 만질 수 있다

---

## 6. v2로 남긴 것

| 항목 | 메모 |
|---|---|
| 카테고리 단위 공유 · 권한 | `category_shares` 추가 + RLS 정책 확장으로 열린다 (01번 9장) |
| 반복 일정의 단일 회차 수정 | 지금은 수정이 전체 반복에 적용된다 |
| Push API 기반 백그라운드 알림 | 발송 서버 필요 |
| 충돌 병합 | 지금은 last-write-wins. 공유가 들어오면 재검토 |
| 사진으로 일정 등록 | Vision 모델 + 키를 숨길 서버 라우트 필요 |

---

## 7. 이 문서의 갱신 규칙

**커밋할 때마다 이 문서를 함께 갱신한다.** 기능이 늘거나 스키마가 바뀌었는데
여기 반영되지 않으면, 다음 사람이 읽는 명세가 코드와 어긋난다.

| 바뀐 것 | 고칠 곳 |
|---|---|
| 기능 추가 · 제거 | 3장 기능 목록 (필요하면 1장에 판단 근거) |
| 스키마 · 타입 변경 | 2장 |
| 성능 · 접근성 · 테스트 수 | 4장 |
| 완료 기준 변동 | 5장 |
| v2 이월 항목 변동 | 6장 |
| 문서만 고친 커밋 | 갱신 불필요 |

머리말의 **기준 커밋 해시**도 함께 고친다.
[01 요구사항](./01-requirements.md)은 착수 전 기록이므로 **고치지 않는다.**

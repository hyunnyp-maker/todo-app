@AGENTS.md

# 문서 규칙

## 요구사항 문서

**커밋할 때마다 `docs/08-v1-final.md`를 함께 갱신한다.** 따로 묻지 말 것.

- `docs/01-requirements.md`는 **착수 전 기획 기록**이다. 고치지 않는다
- 코드가 바뀌면 08번 문서가 실제 명세다. 코드와 어긋난 채로 커밋하지 말 것
- 갱신 대상은 08번 문서 7장의 표를 따른다
  - **기능 추가·제거·변경 → 3장 기능 정의서. 예외 없다**
  - 기능 판단 근거가 바뀌면 1장에도
  - 스키마·타입 변경 → 2장
  - 성능·접근성·테스트 수 → 4장
  - 완료 기준 변동 → 5장
  - v2 이월 항목 변동 → 6장
- 머리말의 **기준 커밋 해시**도 함께 고친다. 커밋 후 실제 해시로 맞출 것
- 예외: 문서만 고친 커밋, README·주석 수정은 갱신 불필요

### 기능 정의서(3장) 작성 규칙

기능을 하나라도 추가·변경하면 **그 커밋 안에서** 3장에 줄을 넣는다. 나중으로 미루지 않는다.

- 표 형식을 지킨다: `ID · 기능 · 사용자가 하는 일 · 시스템이 하는 일 · 규칙·제약 · 상태`
- ID는 영역별 연번(`F7-6`, `F12-4` …). 기존 번호를 재사용하지 않는다
- 새 영역이면 `F13`부터 잇고 3.1 한눈에 보기 표에도 한 줄 추가한다
- 상태는 ✅ 구현 · ❌ 제외 · ⏭ v2 이월 셋 중 하나
- **"규칙·제약" 칸을 비우지 않는다.** 왜 그렇게 동작하는지가 이 문서의 값이다
- 기능을 뺐으면 줄을 지우지 말고 상태를 ❌로 바꾸고 이유를 남긴다
